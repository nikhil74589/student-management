name: prod
on:
  push:
    branches: [main]
jobs:
  deploy:
    uses: ./.github/workflows/reusable-deploy.yml
    secrets: inherit
    with:
      vm: vm-public
      branch: main
      port: "3000"
      environment: production

